<!DOCTYPE html>
<html>
<head>
    <title>Admin Requests</title>
    <style>
body {
    font-family: 'Arial', sans-serif; 
    text-align: center; 
    margin: 0;
    padding: 0;
    background: #f9f9f9; 
}


.container {
    width: 80%;
    margin: auto;
    padding: 20px;
}

h1, h2 {
    color: #333;
    margin-bottom: 10px;
}

.open-requestss {
    transform: translateY(-95%);
}


form {
    display: flex;
    justify-content: center;
    flex-direction: column; 
    align-items: center; 
    margin-bottom: 20px;
}

input[type="text"] {
    padding: 10px;
    font-size: 16px;
    border: 2px solid #ddd;
    border-radius: 4px;
    margin: 10px 0; 
    transform: translateY(-70%);
}


button, .button {
    background-color: #4CAF50;
    border: none;
    color: white;
    padding: 10px 20px;
    text-align: center;
    text-decoration: none;
    display: inline-block;
    font-size: 16px;
    margin: 10px;
    cursor: pointer;
    border-radius: 4px;
    transition: background-color 0.3s;
    transform: translateY(-120%);
}

button:hover, .button:hover {
    background-color: #45a049;
}


.back-link {
    padding: 10px 20px;
    background-color: #4CAF50;
    color: white;
    text-decoration: none;
    border-radius: 4px;
    display: inline-block;
    margin: 20px 0;
    transition: background-color 0.3s;
}

.back-link:hover {
    background-color: #45a049;
}


.open-requests {
    margin: 20px 0; 
    padding: 10px;
    border-radius: 4px;
    transform: translateY(-5%);
}

.open-requests p {
    background: #fff;
    padding: 10px;
    margin: 10px 0;
    border: 1px solid #ddd;
    border-radius: 4px;
}

        </style>
</head>
<body>
    <div class="container"> 
        <h1>Handle Request</h1>
        <h2>Admin Username: <%= username %></h2>
        <br><br>
        <form action='/admin/handle_requests/approval' method="get">
            <label for="requestID">Choose Request from below:</label><br><br>
            <input type="text" id="requestID" name="requestID" placeholder="Enter Request ID:"><br><br>
            <button type="submit">Review</button>
        </form>

        <a href='/admin.html' class="button back-link">Back</a> 

        <h2 class="open-requestss">Open Requests:</h2>
        <div class="open-requests"> 
            <% if (requests.length > 0) { %>
                <% requests.forEach(function(request) { %>
                    <p>Request ID: <%= request.requestID %> => Ticket: <%= request.ticketID %>, Price: <%= request.price %></p>
                <% }); %>
            <% } else { %>
                <p>No open requests.</p>
            <% } %>
        </div>
    </div> 
</body>

</html>
